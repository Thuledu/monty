#include "monty.h"
#include <stdio.h>

/**
  *nop_opcode - program that doesn’t do anything
  *@head: stack the head
  *@counter: the line number
  *Return: void
 */
void nop_opcode(stack_t **head, unsigned int counter) {
    void counter = 0;
    void head = 0;
}
